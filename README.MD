# TinySF

A small interpreted language that only allows you to define a variable and perform mat operations.

## Usage
Just run "make" command, and then use it with a file argument. Use test/test.ty to run it.
